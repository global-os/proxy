import type { Meta, StoryObj } from '@storybook/react-vite'
import { FadingBoxes } from './FadingBoxes'

const meta: Meta = {
  component: FadingBoxes,
  title: 'FadingBoxes',
  parameters: { layout: 'centered' },
}
export default meta

export const Default: StoryObj = {}
